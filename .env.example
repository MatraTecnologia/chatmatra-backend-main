# ─── Banco de Dados ───────────────────────────────────────────────────────────
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=disable
DATABASE_URL="postgresql://matrachat:TROQUE_SENHA@postgres:5432/matrachat?sslmode=disable"

# ─── Better Auth ──────────────────────────────────────────────────────────────
# Gere com: openssl rand -base64 32
BETTER_AUTH_SECRET=TROQUE_POR_UM_SEGREDO_FORTE_32_CHARS

# URL pública do backend (onde o better-auth vai rodar)
BETTER_AUTH_URL=https://api.seudominio.com.br

# OBRIGATÓRIO em produção: domínio raiz com ponto no início.
# Permite que o cookie de sessão seja compartilhado entre subdomínios.
# Ex: frontend em app.seudominio.com.br e backend em api.seudominio.com.br
# → COOKIE_DOMAIN=.seudominio.com.br
COOKIE_DOMAIN=.seudominio.com.br

# ─── URLs ─────────────────────────────────────────────────────────────────────
FRONTEND_URL=https://app.seudominio.com.br
BACKEND_URL=https://api.seudominio.com.br

# ─── SMTP (Email) ─────────────────────────────────────────────────────────────
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=resend
SMTP_PASS=re_SUA_CHAVE_RESEND
SMTP_FROM=MatraChat <noreply@seudominio.com.br>

# ─── Porta do servidor ────────────────────────────────────────────────────────
PORT=3333
